"use client";

export default function FlipCard() {
  // When a card is clicked:
  // - Flip it (with animation D':)
  // - Display a card between 1 and A (random select from an array??) (use only one of the symbols (diamond,hearts,spades,clubs))
}
